[project]
name = "verifiable-llm-se-agents"
version = "0.1.0"
description = "Deterministic verification for LLM-based software engineering agents"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
